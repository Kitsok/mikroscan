scan:
  name: Refresh network
  description: Trigger a Mikroscan refresh using the local API service.
  fields:
    ip_range:
      required: false
      example: "192.168.1.0/24"
      selector:
        text:

generate_topology:
  name: Generate topology
  description: Trigger topology regeneration using the current collected data.
